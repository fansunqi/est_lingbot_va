module: src.data.packed_lerobot.PackedLatentLeRobotDataModule

dataset_path: /path/to/your/dataset  # placeholder: this machine has no libero/demo data; fill in per environment
action_dim: 30
max_tokens: 125440
max_episodes_per_bin: 128
cfg_prob: 0.1
load_worker: 16
seed: 42

train_dataloader:
  batch_size: 1
  persistent_workers: false
  pin_memory: false
